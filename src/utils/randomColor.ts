const colors = [
  "bg-emerald-50 outline-teal-500 text-teal-500",
  "bg-purple-100 outline violet-700 text-violet-700",
];

export const randomColor = colors[Math.floor(Math.random() * colors.length)];
